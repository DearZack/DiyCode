<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.dearzack.diycode">

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Transparent">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".web.WebActivity" />
        <activity android:name=".relate.RelateActivity" />
        <activity android:name=".topicdetail.TopicDetailActivity" />
        <activity android:name=".personal.PersonalActivity" />
        <activity android:name=".reply.ReplyActivity" />
        <activity android:name=".addnews.AddNewsActivity"></activity>
    </application>

</manifest>